This folder contains various tests.

Run tests from the project root with tox:

- `tox -e py310`
- `tox -e py311`
- `tox -e py312`
- `tox -e notebooks` for notebook execution tests only

Use `tox -e format` before `tox -e lint` during development.
